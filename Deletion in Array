#include <stdio.h>

void delete(int arr[], int s, int pos)
{
    int i = pos - 1;

    for (int i = pos; i < s - 1; i++)
    {
        arr[i] = arr[i + 1];
    }

    s--;
}

void print(int arr[], int s)
{
    for (int i = 0; i < s; i++)
    {
        printf("%d ", arr[i]);
    }
}
